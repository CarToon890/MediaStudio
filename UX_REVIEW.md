# บันทึกการทดสอบ UX ของ MediaStudio v1.1.0

วันที่ทดสอบ: 24 กันยายน 2569  
วิธีทดสอบ: เปิดโปรแกรมรุ่น Release ที่กำลังทำงานอยู่ แล้วทดลองใช้งานในมุมมองผู้ใช้ที่ไม่เคยรู้จักโปรแกรมมาก่อน  
สภาพแวดล้อมขณะทดสอบ: Windows, หน้าต่างประมาณ 1180 × 876 พิกเซล, เอนจิน yt-dlp และ FFmpeg พร้อมใช้งาน

## ขอบเขตการทดสอบ

- เปิดดูหน้าเริ่มต้นและอ่านคำแนะนำโดยไม่อาศัย README
- เข้าใช้งานหน้า ดาวน์โหลด, แปลงไฟล์, ตัดวิดีโอ, ตัดและปรับเสียง, คลังงาน และตั้งค่า
- ทดลองกด `ดึงข้อมูล` ขณะที่ยังไม่มีลิงก์
- ทดลองเลื่อนตำแหน่งปัจจุบันบนคลื่นเสียง
- ทดลองเปิดหน้าต่างยืนยันการตรวจสอบและอัปเดตเอนจิน แล้วเลือกไม่ดำเนินการ
- ไม่เริ่มดาวน์โหลด แปลง หรือตัดไฟล์จริง เพื่อไม่สร้างผลลัพธ์ใหม่ระหว่างการประเมิน

โปรแกรมมีไฟล์ `ggez.mp3` อยู่ในคลังงานก่อนเริ่มทดสอบ จึงไม่ใช่สถานะติดตั้งใหม่แบบว่างทั้งหมด ข้อสังเกตเกี่ยวกับการเปิดไฟล์เดิมควรอ่านภายใต้เงื่อนไขนี้

## สรุปภาพรวม

ผู้ใช้ใหม่สามารถเดาโครงสร้างของโปรแกรมได้จากหน้าเริ่มต้นและเมนูด้านซ้าย โดยเข้าใจได้ว่าโปรแกรมแบ่งเป็นการดาวน์โหลด แปลง ตัดวิดีโอ และตัดเสียง หน้าตัดเสียงเป็นหน้าที่สื่อสารสถานะได้ดีที่สุด เพราะมีคลื่นเสียง เส้นเริ่มต้น เส้นสิ้นสุด เส้นตำแหน่งปัจจุบัน เวลา และระดับเสียงอยู่ในบริบทเดียวกัน

สิ่งที่ยังทำให้ผู้ใช้ไม่มั่นใจคือสถานะบางหน้าขัดแย้งกัน การกดคำสั่งที่ข้อมูลไม่ครบแล้วไม่มีข้อความตอบกลับ และคอนโทรลบางรายการดูเหมือนใช้งานได้ทั้งที่ยังไม่ได้เลือกไฟล์ หากแก้สามกลุ่มนี้ก่อน โปรแกรมจะใช้งานได้ตรงไปตรงมาขึ้นมาก

## เส้นทางใช้งานที่ผู้ใช้ใหม่เข้าใจได้

1. เปิดโปรแกรมและตรวจข้อความ `พร้อมใช้งาน` บนหน้าเริ่มต้น
2. เลือกเครื่องมือจากการ์ดกลางหน้าหรือเมนูด้านซ้าย
3. ถ้าต้องการดาวน์โหลด ให้วางลิงก์ กด `ดึงข้อมูล` ตรวจชื่อไฟล์และรูปแบบ แล้วจึงเริ่มดาวน์โหลด
4. ถ้าต้องการใช้ไฟล์ในเครื่อง ให้เลือกหน้าแปลงไฟล์ ตัดวิดีโอ หรือตัดและปรับเสียง แล้วเลือกไฟล์
5. เมื่องานเสร็จ ใช้คลังงานเพื่อเปิดไฟล์ เปิดโฟลเดอร์ หรือส่งต่อไปยังเครื่องมืออื่น
6. ใช้หน้าตั้งค่าเมื่อต้องการเปลี่ยนโฟลเดอร์ผลลัพธ์ การใช้การ์ดจอ หรืออัปเดตเอนจิน

เส้นทางนี้มีอยู่ในตัวโปรแกรมแล้ว แต่ควรแสดงหมายเลขขั้นตอนภายในแต่ละหน้าด้วย โดยเฉพาะหน้า Downloader เช่น `1 วางลิงก์`, `2 ดึงข้อมูลและตรวจชื่อ`, `3 ดาวน์โหลด`

## สิ่งที่ทำได้ดี

### หน้าเริ่มต้น

- บอกภาพรวมสามขั้นตอนตั้งแต่เตรียมเอนจิน เลือกเครื่องมือ จนถึงนำผลลัพธ์ไปใช้ต่อ
- แสดงสถานะเอนจินชัดเจน พร้อมแถบความคืบหน้าและคำอธิบายพื้นที่ว่างที่ต้องใช้
- การ์ดเครื่องมืออธิบายผลลัพธ์ที่ผู้ใช้จะได้รับ ไม่ได้แสดงเฉพาะชื่อทางเทคนิค
- มีงานล่าสุดและทางลัดไปคลังงาน ทำให้แนวคิด ecosystem เริ่มมองเห็นได้

### การนำทางและรูปแบบโดยรวม

- เมนูด้านซ้ายอยู่ตำแหน่งเดิมทุกหน้า และหน้าปัจจุบันใช้สีน้ำเงินแยกจากเมนูอื่นอย่างชัดเจน
- ภาษาไทยเป็นภาษาหลัก และคำสำคัญ เช่น ดาวน์โหลด แปลง ตัดวิดีโอ และตัดเสียง เข้าใจง่าย
- ธีม สี ระยะห่าง และรูปแบบการ์ดมีความต่อเนื่องกัน
- แถบสถานะด้านล่างช่วยยืนยันว่าเอนจินพร้อมใช้งานตลอดเวลา

### หน้าตัดและปรับเสียง

- คลื่นเสียงอ่านง่ายและมีสเกลเวลา
- เส้นเริ่มต้นสีเขียว เส้นสิ้นสุดสีส้ม และเส้นตำแหน่งปัจจุบันสีขาวแยกกันชัดเจน
- การคลิกบนคลื่นเสียงทำให้เส้นตำแหน่งและเวลาปัจจุบันเปลี่ยนตามทันที
- ช่องเวลา ระยะเวลาช่วง รูปแบบผลลัพธ์ และระดับเสียงอยู่ในหน้าเดียวกัน
- ระดับเสียงแสดงทั้งเปอร์เซ็นต์และ dB พร้อมปุ่มกลับเป็น 100%

### หน้าตั้งค่า

- บอกชัดว่าไฟล์จากทุกเครื่องมือจะบันทึกในโฟลเดอร์เดียวกัน
- อธิบายการใช้ NVIDIA NVENC และการกลับไปใช้ CPU เมื่อใช้การ์ดจอไม่ได้
- ปุ่มตรวจสอบและอัปเดตเอนจินมีหน้าต่างถามยืนยันก่อนดาวน์โหลด
- ข้อความยืนยันบอกสิ่งที่จะดาวน์โหลด พื้นที่ประมาณ 150–250 MB และแจ้งว่าจะเก็บไฟล์เดิมไว้หากอัปเดตไม่สำเร็จ

## ปัญหาที่พบและลำดับความสำคัญ

### P1: หน้าแปลงไฟล์แสดงสถานะคิวขัดแย้งกัน

หน้าจอแสดง `มีไฟล์ในรายการ 1 ไฟล์` และปุ่ม `เริ่มทั้งหมด` เป็นสีเขียว แต่บริเวณคิวแสดง `ยังไม่มีไฟล์ในคิว ลากไฟล์มาวางหรือกดเลือกไฟล์`

ผลต่อผู้ใช้: ผู้ใช้ไม่รู้ว่ามีไฟล์พร้อมแปลงจริงหรือไม่ และอาจกดเริ่มงานโดยไม่เข้าใจว่าจะเกิดอะไรขึ้น

ข้อเสนอแนะ:

- ให้ตัวเลขจำนวนไฟล์ ปุ่มเริ่มทั้งหมด และ empty state อ้างอิง collection เดียวกัน
- ปิดปุ่ม `เริ่มทั้งหมด` เมื่อไม่มีรายการที่รอทำงาน
- แสดงรายการเดิมที่ถูกส่งมาจากคลังงานให้เห็นทันที หรือไม่เพิ่มจำนวนจนกว่าจะสร้างรายการในคิวสำเร็จ

### P1: หน้า Downloader ไม่มีข้อความตอบกลับเมื่อกดดึงข้อมูลโดยไม่มีลิงก์

เมื่อช่องลิงก์ว่างแล้วกด `ดึงข้อมูล` หน้าจอไม่มีการเปลี่ยนแปลงหรือข้อความผิดพลาดที่มองเห็นได้

ผลต่อผู้ใช้: ผู้ใช้อาจคิดว่าปุ่มเสีย โปรแกรมค้าง หรือไม่แน่ใจว่าต้องทำอะไรต่อ

ข้อเสนอแนะ:

- แสดงข้อความใต้ช่องว่า `กรุณาวางลิงก์วิดีโอก่อนดึงข้อมูล`
- เปลี่ยนกรอบช่องเป็นสีแจ้งเตือนและย้ายโฟกัสกลับมาที่ช่องลิงก์
- พิจารณาปิดปุ่ม `ดึงข้อมูล` จนกว่าช่องจะมี URL ที่รูปแบบถูกต้อง

### P1: หน้าตัดวิดีโอมีข้อความที่มีความเปรียบต่างต่ำ

ข้อความ `ตัดแบบรักษาคุณภาพเดิมโดยไม่เข้ารหัสวิดีโอใหม่` แสดงเป็นสีเข้มบนพื้นหลังสีเข้ม ทำให้อ่านยากมาก

ผลต่อผู้ใช้: ข้อมูลสำคัญที่อธิบายข้อดีและลักษณะของการตัดวิดีโอแทบมองไม่เห็น และไม่ผ่านหลักการอ่านง่ายของ UI

ข้อเสนอแนะ: ใช้สีข้อความหลักหรือสีรองเดียวกับคำอธิบายบนหน้าอื่น และตรวจ contrast ของข้อความทุกสถานะ disabled/enabled ในธีมมืด

### P2: หน้าตัดวิดีโอแสดงค่าเวลาและคำสั่งข้ามไปจุดนี้ก่อนเลือกไฟล์

ก่อนเลือกวิดีโอ หน้าจอแสดงเวลาเริ่ม 0 วินาที เวลาสิ้นสุด 10 วินาที และความยาวหลังตัด 10 วินาที ปุ่ม `ข้ามไปจุดนี้` ยังดูเหมือนพร้อมกด แม้ตัวเลื่อนและปุ่มเริ่มตัดจะถูกปิดไว้

ผลต่อผู้ใช้: ค่า 10 วินาทีอาจถูกเข้าใจว่าโปรแกรมโหลดไฟล์ตัวอย่างแล้ว หรือเป็นข้อจำกัดของการตัด

ข้อเสนอแนะ:

- แสดง `--:--:--` จนกว่าจะเลือกไฟล์และอ่าน duration สำเร็จ
- ปิดปุ่มข้ามไปจุดเริ่ม/สิ้นสุดพร้อมคอนโทรลอื่นทั้งหมด
- ใช้ empty state เพียงชุดเดียว แล้วค่อยแสดงแผงเวลาเมื่อมีไฟล์

### P2: หน้าตัดเสียงเปิดด้วยค่าที่ไม่ใช่ค่าเริ่มต้นโดยไม่มีคำอธิบายที่มา

ขณะทดสอบ หน้าเสียงเปิด `ggez.mp3` พร้อมจุดเริ่ม 0.882 วินาที ระดับเสียง 35% และตำแหน่งปัจจุบันเดิม ผู้ใช้ใหม่ที่ไม่รู้เรื่องคลังงานอาจไม่เข้าใจว่าไฟล์และค่าเหล่านี้มาจากไหน

ข้อเสนอแนะ:

- แสดงข้อความ `เปิดจากคลังงาน` หรือ `เปิดต่อจากงานก่อนหน้า`
- เมื่อเลือกไฟล์ใหม่ ให้รีเซ็ตช่วงเป็นทั้งไฟล์ ตำแหน่งเป็น 0 และระดับเสียงเป็น 100% เว้นแต่ผู้ใช้เลือกกู้คืนงานเดิม
- ถ้าต้องการจำค่าระหว่างหน้า ให้แสดงสถานะว่าเป็นฉบับร่างที่ยังไม่บันทึก

### P2: คำสั่งนำออกจากคลังอาจถูกเข้าใจว่าเป็นการลบไฟล์จริง

ในรายการคลังงานมีปุ่ม `นำออกจากคลัง` แต่ในบริเวณเดียวกันไม่มีคำอธิบายว่าไฟล์จริงจะยังอยู่

ข้อเสนอแนะ: เปลี่ยนเป็น `ลบออกจากประวัติ` และใส่ข้อความสั้นว่า `ไฟล์ในเครื่องจะไม่ถูกลบ` หากยังใช้คำเดิม ควรมี tooltip หรือหน้าต่างยืนยันที่อธิบายผลให้ชัดเจน

### P2: สถานะสวิตช์ GPU พึ่งสีเพียงอย่างเดียว

สวิตช์ NVIDIA NVENC ใช้สีน้ำเงินบอกว่าเปิดอยู่ แต่ไม่มีคำว่า `เปิด` หรือ `ปิด`

ข้อเสนอแนะ: เพิ่มข้อความสถานะด้านข้าง และหลังตรวจฮาร์ดแวร์ควรบอกผลว่า `พบ NVIDIA NVENC` หรือ `ไม่พบ จึงจะใช้ CPU`

### P3: ปุ่มตรวจสอบและอัปเดตสื่อความหมายสองขั้นตอนในปุ่มเดียว

ชื่อปุ่มทำให้คาดว่าจะตรวจเวอร์ชันก่อน แต่หน้าต่างยืนยันอธิบายว่าจะดาวน์โหลดและแทนที่เอนจิน

ข้อเสนอแนะ: ถ้าระบบไม่ได้ตรวจเวอร์ชันก่อนดาวน์โหลด ให้ใช้ชื่อ `ดาวน์โหลดเอนจินใหม่` หากตรวจได้ ควรแสดงเวอร์ชันปัจจุบันและเวอร์ชันล่าสุดก่อนให้ผู้ใช้ตัดสินใจ

### P3: คำอธิบายปลายทางไฟล์ไม่ปรากฏในหน้าทำงาน

ผู้ใช้ทราบโฟลเดอร์ผลลัพธ์ได้จากหน้าตั้งค่าเท่านั้น

ข้อเสนอแนะ: ใกล้ปุ่มเริ่มงานของแต่ละเครื่องมือ แสดง `บันทึกไปที่ Downloads\MediaStudio` พร้อมปุ่มเปลี่ยนหรือเปิดโฟลเดอร์

## ข้อเสนอแนะสำหรับการนำทางผู้ใช้ใหม่

1. ให้หน้าเริ่มต้นมีปุ่มหลักเพียงปุ่มเดียวตามสถานะ เช่น `เริ่มด้วยการดาวน์โหลด` เมื่อเอนจินพร้อม และวางเครื่องมืออื่นเป็นทางเลือกรอง
2. ในแต่ละหน้า แสดงขั้นตอนปัจจุบันอย่างสั้น เช่น `เลือกไฟล์ → ตั้งค่า → เริ่ม → เปิดผลลัพธ์`
3. ทุกการกดที่ข้อมูลไม่ครบต้องมีผลตอบกลับทันที ไม่ควรปล่อยให้หน้าจอเงียบ
4. หลังงานเสร็จ แสดงปุ่มหลัก `เปิดไฟล์` และปุ่มส่งต่อที่เหมาะกับชนิดไฟล์ พร้อมข้อความว่าไฟล์ถูกเพิ่มเข้าคลังงานแล้ว
5. ใช้คำเดียวกันทั้งระบบ เช่น `คลังงาน`, `คิว`, `ประวัติ`, `นำเข้า` และ `ผลลัพธ์` โดยแยกความหมายให้ชัด
6. เพิ่มปุ่ม `วิธีใช้` หรือไอคอน `?` ที่เปิดคู่มือสั้นตามหน้าปัจจุบัน

## เกณฑ์ตรวจซ้ำหลังแก้ไข

- เปิดโปรแกรมด้วยประวัติว่างและผู้ใช้เข้าใจว่าจะเริ่มตรงไหนภายใน 10 วินาที
- กดดึงข้อมูลโดยไม่มี URL แล้วเห็นคำแนะนำทันที
- หน้าแปลงไฟล์ที่ไม่มีคิวต้องแสดงจำนวน 0 และปิดปุ่มเริ่มทั้งหมด
- หน้าตัดวิดีโอก่อนเลือกไฟล์ต้องไม่มีเวลาจำลองหรือปุ่มที่ดูเหมือนใช้งานได้
- ข้อความทั้งหมดในธีมมืดอ่านได้ชัด โดยเฉพาะข้อความอธิบายโหมดตัดวิดีโอ
- เปิดไฟล์จากคลังงานแล้วทุกหน้าแสดงที่มาของไฟล์และรีเซ็ตค่าที่ควรเริ่มใหม่
- การนำออกจากคลังต้องสื่อชัดว่าไม่ลบไฟล์จริง
- ปุ่มอัปเดตเอนจินต้องถามยืนยันก่อนดาวน์โหลดทุกทางเข้าของคำสั่ง

## ข้อสรุป

โครงสร้างหลักของ MediaStudio เหมาะกับผู้ใช้ทั่วไปแล้ว หน้าเริ่มต้นอธิบายภาพรวมได้ดี เมนูคงที่ และหน้าตัดเสียงมี feedback ที่ชัดเจน จุดที่ควรแก้ก่อนเผยแพร่รุ่นถัดไปคือความถูกต้องของสถานะคิว การแจ้ง validation และความพร้อมใช้งานของคอนโทรล จากนั้นจึงปรับถ้อยคำและเพิ่มคำอธิบายบริบทเพื่อให้ ecosystem ระหว่างเครื่องมือต่าง ๆ เข้าใจได้โดยไม่ต้องอ่านคู่มือภายนอก
